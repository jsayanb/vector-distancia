//METODO PARA REALIZAR ALOGRITMO VECTOR DISTANCIA

    

public void Vector_Distancia(Vertice inicio) {

    for (Vertice vertice : vertices) {
        vertice.setDistancia(Integer.MAX_VALUE);
        vertice.setRuta(null);
    }
    
    inicio.setDistancia(0)
    
    LinkedList<Vertice> cola = new LinkedList<> ();
    cola.add(inicio);
    
    while(!cola.isEmpty()) {
        Vertice vertice = cola.removeFirst();
        
        for (Vertice adjunta : vertice.getAdjuntas()) {
            for (Arista arista : aristas) {
                if (arista.getOrigen().getNom().equals(vertice.getNom()) && arista.getDestino
    
    
